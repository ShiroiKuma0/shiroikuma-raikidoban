<?xml version="1.0" encoding="utf-8"?>
<!-- Row for the "Gesture actions" overview dialog (ActionsOverviewDialog). Text binds to
     ?android:attr/colorForeground so it follows the theme (yellow in night, normal in day). The
     dialog panel background comes from the activity's colorBackgroundFloating (black in night). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:minHeight="?android:attr/listPreferredItemHeight"
    android:paddingLeft="16dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="?android:attr/colorForeground"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/value"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="?android:attr/colorForeground"
            android:alpha="0.7"
            android:textSize="13sp" />

    </LinearLayout>

    <TextView
        android:id="@+id/delete"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:text="4"
        android:gravity="center"
        android:textSize="28dp"
        android:textColor="?android:attr/colorForeground"
        android:clickable="true"
        android:focusable="false"
        android:focusableInTouchMode="false" />

</LinearLayout>
