<resources>

    <style name="AppDark" parent="@style/Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="AppLight" parent="@style/Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="AppDarkNoActionBarWallpaper" parent="AppDark">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowShowWallpaper">true</item>
    </style>

    <style name="AppDarkNoActionBar" parent="Theme.AppCompat.NoActionBar" />

    <!-- DayNight, not .Light: the fork pins night mode, and a hard-coded light parent would ignore it. -->
    <style name="AppLightNoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="AppLightNoActionBarWallpaper" parent="AppLight">
        <!--<item name="android:windowNoTitle">true</item>-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowShowWallpaper">true</item>
    </style>

    <!-- DayNight, not .Light: the fork pins night mode, and a hard-coded light parent would ignore it. -->
    <style name="AppDialog" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <!-- Toolbar theme overlay: dark icons/text on a light bar in day; overridden to yellow-on-black
         in values-night so the Customize / Settings toolbars match the dark theme. -->
    <style name="ToolbarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NoDisplay" parent="AppLightNoActionBarWallpaper">
        <!--<item name="android:windowBackground">@android:color/transparent</item>-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <!-- Base (light) values. The fork pins night mode, so values-night/colors.xml is what actually
         renders; these are kept in step with it so nothing can fall back to stock LL's deep orange-red. -->
    <color name="color_accent">#FFFFFF00</color>
    <color name="color_primary">#FFFFFF00</color>
    <color name="color_primary_dark">#FF000000</color>

    <dimen name="min_handle_content_size">48dp</dimen>
</resources>